/**
 * @deprecated Import from "@/tools" instead.
 * Each tool now lives in its own file under src/tools/.
 *
 * Migration:
 *   import { chatTools } from "@/services/chatTools";
 *   → import { tools } from "@/tools";
 */
export { tools as chatTools } from "@/tools";
